NEXT_PUBLIC_SUPABASE_URL=https://<YOUR_PROJECT_REF>.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=**********
NEXT_PUBLIC_APP_URL=http://localhost:3000
# Chave de Service Role (USO EXCLUSIVO NO SERVIDOR/BACKEND). Nunca exponha no cliente.
SUPABASE_SERVICE_ROLE_KEY=**********

# -- Mercado Pago --
# Token de acesso (APENAS NO SERVIDOR). NUNCA exponha no cliente ou logs.
MP_ACCESS_TOKEN=**********
# Chave pública (pode ser usada no cliente se necessário)
MP_PUBLIC_KEY=**********
# URL da aplicação (servidor) para compor back_urls e notification_url
APP_URL=http://localhost:3000
# Opcional: secret próprio para validar webhook (se você implementar assinatura)
MP_WEBHOOK_SECRET=**********

# -- Resend (E-mail) --
# API Key (APENAS NO SERVIDOR). NÃO exponha no cliente ou logs.
RESEND_API_KEY=**********
# Remetente padrão de e-mails (domínio verificado no Resend)
RESEND_FROM=no-reply@seu-dominio.com.br
# Opcional: reply-to padrão
RESEND_REPLY_TO=atendimento@seu-dominio.com.br
